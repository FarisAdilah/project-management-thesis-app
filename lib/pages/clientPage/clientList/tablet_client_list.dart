import 'package:flutter/material.dart';

class TabletClientList extends StatelessWidget {
  const TabletClientList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Tablet Client List"),
      ),
    );
  }
}
