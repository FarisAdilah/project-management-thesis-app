import 'package:flutter/material.dart';

class TabletMainPage extends StatelessWidget {
  const TabletMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tablet Main Page'),
      ),
    );
  }
}
