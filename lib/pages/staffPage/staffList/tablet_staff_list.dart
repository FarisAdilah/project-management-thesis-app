import 'package:flutter/material.dart';

class TabletStaffList extends StatelessWidget {
  const TabletStaffList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Tablet Staff List"),
      ),
    );
  }
}
