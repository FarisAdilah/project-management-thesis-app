import 'package:flutter/material.dart';

class TabletVendorList extends StatelessWidget {
  const TabletVendorList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Tablet Vendor List"),
      ),
    );
  }
}
