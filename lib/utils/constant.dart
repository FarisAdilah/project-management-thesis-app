enum UserType { supervisor, admin, projectManager, staff }

enum CollectionType { users, projects, timelines, scheduleTasks }

class Constant {
  static const bool showLog = true;
  static const String logIdentifier = "PenTools-log";
}
